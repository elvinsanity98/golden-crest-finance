<header class="bg-slate-900 text-white sticky top-0 z-30 shadow-lg">
  <div class="flex items-center justify-between px-4 sm:px-6 lg:px-8 h-16">
    <div class="flex items-center gap-3">
      <button id="sidebarToggle" class="lg:hidden p-2 -ml-2 rounded hover:bg-slate-800" aria-label="Menu">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
      </button>
      <a href="/" class="flex items-center gap-2">
        <span class="bg-gradient-to-br from-brand-400 to-brand-600 text-slate-900 rounded-lg w-9 h-9 flex items-center justify-center font-extrabold shadow">GC</span>
        <div class="hidden sm:block leading-tight">
          <div class="font-bold text-base"><%= brand.name %></div>
          <div class="text-[10px] uppercase tracking-wider text-brand-300">10% / month · Daily</div>
        </div>
      </a>
    </div>
    <div class="flex items-center gap-2">
      <% if (typeof currentUser !== 'undefined' && currentUser) { %>
        <div class="text-right hidden sm:block leading-tight">
          <div class="text-sm font-semibold"><%= currentUser.fullName %></div>
          <div class="text-[10px] uppercase tracking-wider text-slate-400"><%= currentUser.role %></div>
        </div>
        <form action="/logout" method="POST" data-confirm="Log out of Golden Crest Finance?" class="ml-1">
          <button class="px-3 py-1.5 text-sm rounded-md bg-slate-800 hover:bg-slate-700 border border-slate-700">Logout</button>
        </form>
      <% } else { %>
        <a href="/login" class="px-3 py-1.5 text-sm rounded-md bg-brand-500 hover:bg-brand-400 text-slate-900 font-semibold">Login</a>
      <% } %>
    </div>
  </div>
</header>
